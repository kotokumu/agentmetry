## 1. Lifecycle foundation and storage generation

- [x] 1.1 `[[telemetry-retention/retention-policy]]` Add validated policy, cutoff, hold, retention-state, eligibility, restore-scope, segment-reference, cycle, operation, and integrity domain values; verify all accepted and rejected partitions, exact cutoff/hold boundaries, and fixed-instant age calculations with table-driven unit tests. (`go test ./internal/retention`)
- [x] 1.2 `[[otlp-ingestion/lossless-raw-export-retention]]` Add stable Retained Export identity allocation so new `payload_occurrence` values equal globally increasing export IDs and restores preserve explicit historic IDs/occurrences; verify duplicate-payload admission across archive, restore, deletion, restart, and legacy fixtures never reuses a uniqueness tuple.
- [x] 1.3 `[[telemetry-retention/lifecycle-integrity-and-recovery]]` Add the lifecycle-aware SQLite generation with policy, retained export, cycle, operation target, current authority, segment history/current membership, replacement, deletion-evidence, and durable-phase relations; verify constraints, one authority per export, exact current membership, self-contained Deleted rows, sequence seeding, and foreign-key integrity against migrated fixtures.
- [x] 1.4 `[[telemetry-retention/automatic-archive]]` Add export ownership to all Active-derived evidence and complete Active-only span projection candidates; verify replay migration preserves current sessions, traces, logs, metrics, links, costs, and retry-winner projections without heuristic timestamp matching.

## 2. Archive representation and automatic archive

- [x] 2.1 `[[telemetry-retention/lifecycle-integrity-and-recovery]]` Implement deterministic versioned tar framing with whole-segment zstd compression, streaming decode, bounded member allocation, member/membership/file digests, and durable candidate installation; verify v1 golden round trips exact pre-normalization protobuf and replay metadata and rejects corrupt lengths, hashes, membership, codecs, and unsupported versions.
- [x] 2.2 `[[telemetry-retention/automatic-archive]]` Implement fixed-date and bounded-size segment planning plus Active export streaming; verify segments never split an export, respect UTC receive-date and 256 MiB original-byte bounds, and exploit one compression stream across multiple exports.
- [x] 2.3 `[[telemetry-retention/automatic-archive]]` Implement the single SQLite archive publication transaction through `LifecyclePublisher`; verify policy/hold revalidation, verified file authority, current and historical membership, Active raw/projection removal, aggregate and change-feed reconciliation, and one Active-or-Archived reader-visible result under injected failures.
- [x] 2.4 `[[telemetry-retention/retention-scheduling-and-status]]` Implement startup and 24-hour scheduling with fixed-cohort parent cycles and child archive/delete operations; verify concurrent admission waits for a later cycle, overlapping cycles cannot share export authority, setup failure and zero-work/mixed-child terminal propagation are exact, and a later cycle runs after failure.

## 3. Inventory, capacity, and restoration

- [x] 3.1 `[[telemetry-retention/archive-inventory-and-capacity]]` Implement bounded current-segment inventory, terminal segment lookup, scheduled deletion calculation, payload/metadata integrity reporting, and capacity accounting; verify pagination, disabled/unverifiable schedule reasons, non-overlapping SQLite page classes, OS allocation, freelist and filesystem availability, estimate warnings, and that capacity never grants expiry.
- [x] 3.2 `[[telemetry-retention/archive-restoration]]` Implement segment and half-open receive-time selection, lifecycle-state classification, exact affected-set calculation, and transactional restore authority claims; verify boundary-cut periods, mixed Active/Archived/Restoring/Deleted matches, terminal/unknown segment lookups, restore conflicts, restore preemption of pre-cutover expiry, and repeated Active requests leave holds unchanged.
- [x] 3.3 `[[telemetry-retention/archive-restoration]]` Implement the OTLP `ExportReplayer` preparation adapter and atomic SQLite restore publication using explicit identities; verify current normalizer success and failure, no prepared-state visibility, finite hold activation only at publication, and all-or-none failure for corrupt, unsupported, insufficient-capacity, and storage-error sets.
- [x] 3.4 `[[telemetry-retention/archive-restoration]]` Implement full-segment restored references and partial-segment replacement publication; verify replacements contain every unselected member exactly once, no selected member, preserve verified Archived authority, form only acyclic terminating paths, and resolve original segment IDs transitively.

## 4. Automatic expiry and recovery

- [x] 4.1 `[[telemetry-retention/automatic-archive-expiry]]` Implement whole-segment deletion eligibility and staging from current membership; verify mixed-age segments remain Archived, Active data is never directly deleted, corrupt payload with verifiable membership can expire, unverifiable membership cannot, and current policy is revalidated.
- [x] 4.2 `[[telemetry-retention/automatic-archive-expiry]]` Implement SQLite-owned deletion cutover under the shared `writeGate`, with staged, `delete_committing`, `content_removed`, and terminal phases and a passive durable file handle; verify restore wins before the serialized point of no return, deletion wins after it, and no policy update or restore claim interleaves between final authority check and terminal publication.
- [x] 4.3 `[[telemetry-retention/lifecycle-integrity-and-recovery]]` Implement startup recovery before services become ready for archive, restore, replacement, and deletion phases; verify every durable phase boundary plus representative file sync, rename/removal, SQLite publication, cancellation, and capacity failures converge idempotently to one truthful authority without partial query state.
- [x] 4.4 `[[telemetry-retention/automatic-archive-expiry]]` Persist self-contained non-content Deletion Evidence and prune detailed terminal operation rows beyond 100; verify period and segment restore lookups still report deletion after the authorizing operation record is pruned and no restorable content remains.

## 5. Local API and Web experience

- [x] 5.1 `[[telemetry-retention/retention-policy]]` and `[[telemetry-retention/retention-scheduling-and-status]]` Extend the protobuf SSOT and generated Go/TypeScript Connect clients with policy and status RPCs; verify Buf lint/breaking checks, presence validation, every running cycle, newest-100 terminal cycle history, all pending/running operations, and newest-100 terminal operation ordering.
- [x] 5.2 `[[telemetry-retention/archive-inventory-and-capacity]]` and `[[telemetry-retention/archive-restoration]]` Add bounded inventory, capacity, and asynchronous restore RPC adapters; verify invalid scopes/holds, integrity/capacity errors, terminal lookups, paging, and durable acknowledgement mapping with Connect tests.
- [x] 5.3 `[[telemetry-retention/retention-policy]]`, `[[telemetry-retention/archive-inventory-and-capacity]]`, and `[[telemetry-retention/archive-restoration]]` Add the localized Web retention settings, capacity summary, inventory, restore dialogs, and operation polling; verify enable/disable validation, segment/period restore, finite holds, warning/error states, accessibility, and absence of manual-delete or archived-query controls with component tests.

## 6. Verification and documentation

- [x] 6.1 `[[otlp-ingestion/lossless-raw-export-retention]]` and `[[telemetry-retention/lifecycle-integrity-and-recovery]]` Run migration equivalence and recovery tests against representative legacy and lifecycle-aware databases; record journal digests, Active query equivalence, archive-file verification, stable identities, and successful pre-service recovery.
- [x] 6.2 `[[telemetry-retention/retention-policy]]`, `[[telemetry-retention/retention-scheduling-and-status]]`, `[[telemetry-retention/automatic-archive]]`, `[[telemetry-retention/archive-inventory-and-capacity]]`, `[[telemetry-retention/archive-restoration]]`, `[[telemetry-retention/automatic-archive-expiry]]`, and `[[telemetry-retention/lifecycle-integrity-and-recovery]]` Execute every specified Scenario and record the observable result, including boundary, error, concurrency, idempotency, compatibility, and corruption cases.
- [x] 6.3 `[change]` Run focused Go package tests, `go test ./...`, `go test -tags=integration ./...`, `npm --prefix web test -- --run`, `npm --prefix web run build`, Buf generation/lint/breaking checks, and repository lint; resolve regressions and record actual results.
- [x] 6.4 `[design-only]` Update user and operator documentation for policy semantics, raw-only archives, restore holds, capacity terminology, deletion irreversibility, file location/permissions, and recovery behavior; verify every claim links to the implemented contract or an executable test and no manual deletion, archived search, encryption, or capacity-triggered deletion is implied.
- [x] 6.5 `[change]` Run `openspec validate add-telemetry-archive-retention --strict`, resolve every error and all P0/P1 findings from `openspec/specs/REVIEW.md`, and verify implementation plus documentation match every checked task before publication.
