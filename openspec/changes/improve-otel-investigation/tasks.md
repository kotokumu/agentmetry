## 1. 先行確認：OTel の内容と既存契約（調査・文書のみ）

この一覧は未着手の作業を示す。候補 1〜6 は章 2〜7 に対応する。章 1 の結果を使って入力表示の対応範囲を決め、章 2・3 を先行し、章 4〜7 を順次実施する。

- [x] 1.1 Claude Code の既存 source 仕様・profile・代表 OTLP fixture を照合し、入力、応答、tool 入出力、AGENTS.md/Skill 等の参照について raw/投影/未報告/意味未確認を整理する。完了条件：各項目に受信 field・出典・対応 ID・本文の可用性を付けた調査表がある。送信されない項目も明記する。
- [x] 1.2 Codex について同じ調査表を作成する。完了条件：logs と trace-safe event の本文の差、秘匿マーカー、tool の arguments/output を分けて記録し、Claude Code の mapping と混同していない。
- [x] 1.3 既存 Web 比較の 5 指標、MCP `compare_runs` の 1〜10 件の集計契約、navigation/anchor、本文取得の制限を確認する。完了条件：現行のテスト名と保つべき入出力の一覧があり、変更対象と互換性維持対象が区別されている。

---

## 2. 候補1：問題箇所へ直接移動する

依存：1.3。完了条件：指定された失敗箇所を一意に開き、元の調査状態へ戻れる。

- [x] 2.1 trace/span ID で対象を含む activity 範囲を取得できる query と追加 read contract を実装する。完了条件：複数失敗、現在のページ外、存在しない対象を扱う query/API テストが通り、別の span を代替選択しない。
- [x] 2.2 根拠リンクと trace URL に対象 span を渡し、選択・展開・focus と戻り状態を接続する。完了条件：navigation/UI テストで source、会話、agent、filter、位置が復元され、既存の会話内 span URL も使える。
- [x] 2.3 手戻りエピソードの上位 3 件以降を UI から段階表示する。完了条件：5 エピソードの fixture で全件と根拠操作を確認でき、partial analysis の表示が維持される。

---

## 3. 候補2：Web/MCP の診断比較を揃える

依存：1.3。完了条件：固定データの同じ 2 会話に対し、両経路で指標・分母・欠測の意味が一致する。

- [x] 3.1 現行 Web の入力/期待値を基準に Go query の比較契約と動作テストを作り、5 指標・差・単位・coverage・可否判定を実装する。完了条件：分母ゼロ、未報告、partial、同一会話、異なる source、時間重複、不正時刻を検証するテストが通る。
- [x] 3.2 比較中に対象の読み取り結果が混在しない取得処理を実装し、protobuf/Connect の追加 read API と生成物を更新する。完了条件：更新中の fixture と adapter テストで整合した結果を返し、既存 API のテストが通る。
- [x] 3.3 read-only MCP `compare_rework` と利用案内を追加する。完了条件：baseline/current の source と run ID を明示して比較でき、既存 `compare_runs` の 1〜10 件・集計 field・異なる source を含む利用が維持され、本文は既定で含まれない。
- [x] 3.4 Web Before/After を共有比較結果の表示へ切り替え、重複する式を除く。完了条件：固定 fixture で Web/MCP の 5 指標・分母・理由が一致し、丸め・既存 harness 表示・未対応サーバー時の状態を UI テストで確認する。

---

## 4. 候補3：入力・コンテキスト・実行本文を読みやすくする

依存：1.1〜1.3、2.2、3.4。受信済み projection の範囲で完了させ、未対応の raw 内容を待って保留しない。

- [x] 4.1 会話内に「実行」「手戻り」「比較」の view を設け、選択と履歴状態を接続する。完了条件：view の切替と戻る操作で会話・agent・選択 activity を失わない UI テストが通る。
- [x] 4.2 実行行を概要表示にし、選択した本文・metadata を隣接詳細へ表示する。完了条件：通常幅と狭幅で入力/応答/tool の既存本文が読め、長文・本文なし・live 到着でも選択を維持する。
- [x] 4.3 1.1・1.2 で確認した既存 field から本文種別・受信出典・証拠の種類を表示する。完了条件：参照先だけ、read/tool の本文、明示されたモデル入力、種類不明を区別し、ファイル参照だけでモデル入力と断定しないテストが通る。未対応の AGENTS.md/Skill 本文を生成・別途取得しない。
- [x] 4.4 大見出し・装飾・小さいラベルの階層を整理し、失敗・選択・欠測・本文を識別しやすくする。完了条件：キーボード操作、visible focus、200% 拡大、reduced motion と狭幅で本文・戻り操作が利用できることをブラウザで確認する。

---

## 5. 候補4：構造化フィルタと保存条件

依存：1.3、2.2。完了条件：条件が全対象集合に適用され、再読込後も同じ条件で調査できる。

- [x] 5.1 observed failure、会話の経過時間下限/上限、model、tool を query/read API に追加する。完了条件：ページ外の一致、未知の outcome、欠けた時刻、会話内の別 activity にある model/tool、重複のない結果を検証するテストが通る。
- [x] 5.2 Web の定型 filter と URL/履歴の状態を接続する。完了条件：source・時間・自由文との AND 条件がサーバー結果と一致し、不正条件を適用せず、最後に適用した条件と編集中の条件を区別できる。
- [x] 5.3 名前付き保存条件の保存・適用・置換・削除を Web profile に実装する。完了条件：再読込、相対 24h の再評価、未対応の保存形式、破損データ、保存失敗をテストし、条件の黙った欠落や保存成功の誤表示がない。

---

## 6. 候補5：取得範囲と欠測を説明し、保存済みデータを調べる

依存：1.1・1.2、3.1、4.3。投影拡張の具体的な mapping はこの作業の調査結果として判断する。

- [x] 6.1 query/API と Web 表示で、分析済み projection の範囲、本文の可用性、表示 filter を別の状態として扱う。完了条件：全 projection 分析済みでも本文が未報告、明示的な秘匿、filter で非表示、意味未確認の例が UI/MCP で矛盾しない。
- [x] 6.2 保存済み raw の span event/attributes と現行 projection の差を、代表 fixture で検証する。完了条件：本文・対応 ID・時刻・usage の追加利用候補と二重計上の可能性を記した調査結果があり、未知の内容は raw 保持のまま扱う。
- [x] 6.3 6.2 の結果から投影拡張の採否を文書化する。完了条件：価値がある mapping は対象 provider・出典・identity・重複・既存 raw への適用範囲・`otlp-ingestion` の変更要件を持つ追跡可能な後続タスクにする。採用候補がなければ根拠付きで終了し、OTel 外の収集へ置き換えない。

---

## 7. 候補6：長い trace のタイムラインを改善する

依存：2.1・2.2、6.1。完了条件：全体の範囲と取得状況を失わず、対象区間を調べられる。

- [x] 7.1 本文を含めない trace 概要と時間範囲の詳細取得を query/read API に追加する。完了条件：12/200/1200 activities、欠損親、範囲外 span、partial 結果をテストし、詳細ページの件数を全体件数として扱わない。
- [x] 7.2 概要・拡大/縮小・種別/エラー filter を waterfall に接続する。完了条件：長い待ち・並列 span を持つ fixture で対象区間を選べ、全体時刻・選択・戻り状態を維持し、filter 解除で対象を再表示できる。
- [x] 7.3 live 更新と狭幅・キーボード操作を調整する。完了条件：新着 span で読んでいる範囲が強制移動せず、未取得と filter 非表示を識別でき、最長区間と並列箇所を固定 fixture 上で正しく指せる。

---

## 8. 文書の更新（実装と分離）

- [x] 8.1 各作業群の完了後に、read API・MCP の利用案内と操作説明を実装に合わせて更新する。完了条件：既存 `compare_runs` と新しい診断比較の用途、受信内容の限界、相対時間の保存条件を説明し、コードに存在しない機能を完了扱いしない。
- [x] 8.2 source ごとの内容可用性と raw 活用の調査結果を反映する。完了条件：出典・確認した版・fixture の実在を確認し、上流の wire format と agentmetry の製品要件が区別される。

---

## 9. 統合検証と計画の完了確認

- [x] 9.1 対象の Go/Vitest テストに加え、`go test ./...`、`go test -tags=integration ./...`、`npm --prefix web test -- --run`、`npm --prefix web run build` を実行する。完了条件：結果を記録し、既存動作の回帰を解消する。有料の実エージェント E2E は必須にしない。
- [x] 9.2 合成 telemetry の一時 DB で「filter → 根拠 span → 入力/コンテキスト → 戻る → 比較」の一連の操作を確認する。完了条件：Web/MCP の比較が同じ snapshot で一致し、source-qualified identity・欠測・選択が保たれることを記録する。
- [x] 9.3 `openspec validate improve-otel-investigation --strict` を実行し、実装済みの内容と未着手/後続タスクを照合する。完了条件：strict validation が成功し、各チェックボックスは実際の検証結果に基づく。計画ファイルの存在だけで実装完了にしない。
